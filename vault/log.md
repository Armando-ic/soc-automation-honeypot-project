# Log

Append-only chronological record of meaningful actions in this project. One line per entry. Backfilled entries are marked.

---

2026-04-27 [backfilled] — Splunk Universal Forwarder configured on Windows 10 VM; Splunk receiving Windows event logs at index `mydfir-project`
2026-04-27 [backfilled] — n8n SOC triage workflow built per video tutorial; AbuseIPDB + VirusTotal + DFIR-Iris + Slack wired to Claude Opus 4.7 via Anthropic node
2026-04-27 [backfilled] — DFIR-Iris stood up at https://192.168.129.133
2026-04-27 [backfilled] — Splunk MCP installed in Claude Desktop per video tutorial; tested with multi-day investigation queries
2026-04-27 [backfilled] — Splunk MCP mirrored to Claude Code (VS Code) at local scope; see [[runbooks/splunk-mcp-setup]] and [[decisions/0004-mirror-mcp-to-claude-code]]
2026-04-27 — `SOC-Automation-Project.md` added to `.gitignore` (contained Claude API key, VirusTotal key, DFIR-Iris admin password, VM passwords)
2026-04-27 — Vault initialized at `vault/`; see [[decisions/0001-vault-structure]]
2026-04-27 — Brainstorm opened for Sub-project A1 (Structured Outputs); see [[subprojects/2026-04-27-structured-outputs/README]]
2026-04-27 — Brainstorm completed for A1; spec written and self-reviewed at [[subprojects/2026-04-27-structured-outputs/spec]]; awaiting user approval before writing implementation plan
2026-04-27 — Spec approved; implementation plan written at [[subprojects/2026-04-27-structured-outputs/plan]]; user chose pair-execution mode
2026-04-27 — A1 implementation in progress: vault scaffold redacted of plaintext passwords, repo initialized via git, AbuseIPDB credential created, workflow duplicated as `SOC Triage v1`, all three tools migrated/renamed, system prompt rewritten and moved to `system` role, user message JSON.stringify fixed, submit_triage_result Code Tool added with v1 JSON Schema, Extract Triage Result Code node added, Slack + DFIR-Iris consumers wired to structured fields, Splunk URL hostname patched, operational settings configured (120s timeout, retry on fail), all three pinned test cases passed
2026-04-28 — A1 cutover completed: Splunk webhook URL updated to point at v1 production URL, workflow published, end-to-end verified with real failed RDP attempts. Slack and DFIR-Iris both received structured alerts with dynamic severity. Splunk saved search disabled after verification. A1 complete; see [[subprojects/2026-04-27-structured-outputs/runbook]] for operations and [[subprojects/2026-04-27-structured-outputs/notes]] for the issue/learning record.
2026-04-28 — Brainstorm opened for Sub-project A2 (Iris Escalation Gate); see [[subprojects/2026-04-28-iris-escalation-gate/README]]
2026-04-28 — Brainstorm completed for A2; spec written at [[subprojects/2026-04-28-iris-escalation-gate/spec]]; awaiting user review before writing implementation plan
2026-04-30 — A2 complete: Iris Escalation Gate shipped end-to-end. All 5 pinned tests + e2e Approve path verified against real Splunk traffic (Iris alerts #47/#48/#50 -> cases #11/#12/#13). Phase 12 documentation closure: runbook at [[subprojects/2026-04-28-iris-escalation-gate/runbook]], spec at [[subprojects/2026-04-28-iris-escalation-gate/spec]] (with Errata section for in-flight design corrections), ADR at [[decisions/0005-additive-ioc-type-schema-enhancement]], component docs updated at [[architecture/components/dfir-iris]] (escalate handler None bugs) and [[architecture/components/n8n]] (signed Wait-resume URLs, timeout-not-flagged). A3 (Splunk lookup blocklist) is the next sub-project and the moment the schema bumps to v2.
2026-04-30 — Sequencing decision: D1 (Detection foundations — Sysmon + Atomic Red Team + Splunk SPL practice) pulled forward ahead of A3. Rationale at [[architecture/target-state#sequencing-decision-2026-04-30]]; brainstorm not yet opened. Vertical-slice scope: one MITRE technique -> one Sysmon-driven Splunk saved search -> one integration with the n8n webhook. Driven by the project's job-prep frame (SOC analyst interviews drill on detection content, EventCodes, SPL — currently only the brute-force saved search exists).
2026-04-30 — Brainstorm completed for Sub-project D1 (Detection Foundations); spec + README written at [[subprojects/2026-04-30-detection-foundations/spec]] / [[subprojects/2026-04-30-detection-foundations/README]]; awaiting user review before writing implementation plan. Locked decisions: Option A integration shape (reuse existing webhook, gate-skipped path expected, no n8n changes), T1059.001 PowerShell encoded command as worked example (foundation is the deliverable, technique is the integration test), SwiftOnSecurity off-the-shelf Sysmon config, manual ART invocation (no harness), `vault/detections/` as new catalog directory, new `vault/architecture/components/sysmon.md` page for long-lived reference (EventCode + field tables), saved search settings mirror brute-force (Time Range Last 24h + Trigger For each result + Webhook+Triggered Alerts) with cron `*/5 * * * *` (standard SOC cadence; brute-force's `* * * * *` was a test value).
2026-04-30 — Spec approved for D1; implementation plan written at [[subprojects/2026-04-30-detection-foundations/plan]]; user chose inline-pair-execution mode mirroring A1/A2 cadence
2026-04-30 — D1 implementation in progress: Phase 0 surfaced two pre-existing realities (UF inputs.conf already had Sysmon stanza with `source=XmlWinEventLog:` override, dating to 2026-04-25; Windows VM SSH server wasn't enabled — installed OpenSSH-Server during Phase 0); Splunk Add-on for Microsoft Sysmon (5.0.0) installed during Phase 0 → ADR 0006 written. Phases 1-3 installed Sysmon 15.20 + SwiftOnSecurity config commit `1836897f` + ART module v2.1.0 + atomics library (334 techniques). Tier-1 smoke tests #1, #2, #3 all PASS. Phase 4 hand-crafted T1059.001 SPL against a real T1059.001-15 ATH event (CommandLine `powershell.exe -NoProfile -E <base64>`, parent `WmiPrvSE.exe`, Hashes triple all populated). Phase 5 created the Splunk saved search `T1059.001 - PowerShell Encoded Command` (cron `*/5 * * * *`, Time Range Last 24h, Trigger For each result, webhook to v2 production URL — required `realtime_schedule=False` to keep `is_scheduled=True` in Splunk 10.2.2). Phase 6 live-fire PASS — Iris alert #51 created end-to-end (iocs=[], gate-skipped path Outcome A, Claude correctly identified WMI parent as forensic indicator + decoded the base64 payload independently with no system-prompt addendum). Phase 6.4 dedup verification surfaced a real spec-divergence: Splunk's "For each result" + `alert.suppress=False` does NOT dedupe across cron ticks — same row re-fires every tick. Three production-tuning options documented in runbook. Phase 7-9 seeded `vault/detections/` (template + index + worked-example page populated with real captured data), created `vault/architecture/components/sysmon.md` (long-lived reference: install metadata, EventCode + field tables, ParentImage cheat sheet, gotchas), updated `splunk.md` + `CLAUDE.md` schema + `index.md`, wrote runbook covering install + run-a-technique loop + 7 starter SPL queries + 7 named recovery ladders + standby system-prompt-addendum (reactive-only; not applied during D1).
2026-04-30 — D1 complete: Sysmon + ART + observation lab live; one worked-example saved search firing the existing v2 webhook on a 5-minute cron; `vault/detections/` catalog seeded; `sysmon.md` component page is the long-lived reference. Architectural promise (Sysmon-shaped alert traverses A2's Test 1 path on real production traffic with no n8n changes) validated. Six surfaced gotchas now documented in runbook recovery ladders for future-fresh-instance / future technique runs. Spec.md Errata captures E1-E6 (XmlWinEventLog source prefix, verify-only Phase 2, PowerShell/Defender source collateral, regex broadening, host-not-ComputerName, Test-15-not-Test-2). ADR 0006 records the Splunk Add-on for Microsoft Sysmon installation. Open follow-ups: (a) severity-stamping inconsistency between Claude's prose and Iris stored severity_id (alert #51 said "medium" → stored 1, alert #52 said "high" → stored 5); flagged for D1.5/A3 era investigation. See [[subprojects/2026-04-30-detection-foundations/runbook]] for operations and [[subprojects/2026-04-30-detection-foundations/notes]] for the issue/learning record. Successors unlocked: A3 (Splunk lookup blocklist), C (detection-engineering at scale), H (automated purple team), B (EDR layer).
2026-05-08 — OneDrive incident + recovery: User's OneDrive cleanup deleted ~194 VM-related files from `C:\Users\Owner\OneDrive\Documents\Virtual Machines\`. Root cause was structural: Microsoft's "back up Documents" + VMware's default "VMs in Documents" → OneDrive had been making `(1).vmdk` conflict copies of running VM disks for weeks. vmware.log showed `FileLockMemberValues read failure`/`SnapshotConfigInfoLockVmsd: Insufficient permissions` — UF/VMware couldn't get exclusive locks because OneDrive's Cloud Files filter held them. Recovery: stopped OneDrive process + helpers, parsed `C:\$Recycle.Bin\<SID>` `$I/$R` pairs to identify deleted-file source paths, classified surviving on-disk files as locally-cached vs `Offline` placeholders (attr `0x401520` = `RECALL_ON_DATA_ACCESS|OFFLINE`), built unified copy combining locally-cached + bin items, migrated MyDfir-Windows10 (63 files, 48 GB) and MyDFIR-Splunk (84 files, 18 GB) to `F:\VMs\`. Updated `inventory.vmls` + `preferences.ini` (backed up to `.bak.<ts>`) to swap OneDrive paths → F: paths. MyDFIR-Splunk reverted cleanly to `Splunk-installed` snapshot (pre-cloud-only-vmem loss; cold boot from base disk; ext4 clean; service active; ports 8000/8089/9997 listening). MyDfir-Windows10 hit a snapshot-chain CID mismatch (`parentCID=154ec010` ≠ base `CID=b81ad97c`); fixed by promoting the OneDrive-conflict-copy descriptor `MyDfir-Windows10-000001(1).vmdk` (matching `parentCID=b81ad97c`) to canonical name. VM booted from RAM-state Snapshot2 once but cold-boot revealed deeper Frankendisk corruption (multiply-claimed blocks across base disk extents — s001-s007 from OneDrive locally-cached + s008-s016 from Recycle Bin restored captured at different points in time). Resolution: rebuilt as **MyDfir-Windows10-v2** (DESKTOP-VNEF7PC, 192.168.129.130, mydfir/[REDACTED — see SOC-Automation-Project.md]). Re-ran D1 install: Sysmon 15.20 + SwiftOnSecurity commit `1836897f` (config SHA256 matches sysmon.md baseline), inputs.conf with all four Windows channel stanzas (Security/Application/System/Sysmon), UF service swapped to `LocalSystem` (default `NT SERVICE\SplunkForwarder` lacked Sysmon channel ACL → `errorCode=5`), `Splunk_TA_microsoft_sysmon` reinstalled on indexer (was lost in snapshot revert), ART module + atomics 332 techniques + powershell-yaml dependency, `Invoke-AtomicTest T1059.001 -ShowDetailsBrief` catalog loads. End-to-end smoke verified: Sysmon EventCode=1 notepad event → UF → Splunk → field extraction (`Image`, `ParentImage`, `CommandLine` populated). New canonical revert points: `D1-fully-installed-2026-05-08` on Win10-v2, `D1-baseline-restored-2026-05-08` on Splunk; old `D1 pre-install baseline` and `Splunk-installed` snapshots are stale on the corrupted Win10 (deleted) and the still-functional Splunk respectively. Drive migration: Win10-v2 + Zeek-Suricata (user-created, instructional-video walkthrough) moved from F:\VMs (USB HDD; PSGallery `Install-Module powershell-yaml` hung 15+ min, likely contributed to a WHEA_UNCORRECTABLE_ERROR BSOD via virtualization timing starvation) to `C:\VMs\` (NVMe; same `Install-Module` then completed in 2.3s). Splunk daemon stays on F:. Open follow-ups: (a) **OneDrive aftercare** — opt out of Documents folder backup so this can never recur; decide fate of still-trapped lower-priority VMs (n8n@.132, IRIS@.133, legacy `Splunk`, legacy `Windows 10 x64`) sitting in `OneDrive\Documents\Virtual Machines\` — same recovery recipe applicable when needed. (b) **Cold-VM backup strategy** — non-OneDrive replacement (OVF export to F: of powered-off VMs, or scheduled robocopy of `C:\VMs\` snapshot trees). (c) **Win10 stale library entry** — `inventory.vmls` `vmlist3` still points at deleted `F:\VMs\MyDfir-Windows10\`; user removes via VMware UI right-click → Remove from Library. See `feedback_vm_storage_location.md` and `reference_host_drive_layout.md` in `~/.claude/projects/.../memory/` for the durable rules learned.
2026-05-12 — n8n + IRIS rebuilt from scratch (the OneDrive aftercare item from 2026-05-08 closed for these two VMs). Sequencing: chose Y (full rebuild → re-validate → freeze) over X (freeze on synthetic evidence, defer rebuild). Ubuntu Server 24.04 ISO at `C:\Users\Owner\Desktop\MYDFIR\ubuntu-24.04.4-live-server-amd64.iso`. Built as **MyDFIR-n8n-VM-v2** (192.168.129.132) and **MyDFIR-DFIR-IRIS-VM-v2** (192.168.129.133), both at `C:\VMs\` (NVMe, per VM-storage memory rule), 2 cores / 4–8 GB RAM / 50 GB disk each. Per-VM build via paramiko SSH writes (pre-approved scope per `feedback_ubuntu_vm_ssh_rebuild.md`): apt update/upgrade → static IP pinned via netplan (cloud-init network config disabled at `/etc/cloud/cloud.cfg.d/99-disable-network-config.cfg`) → `apt install docker.io docker-compose git`. n8n compose: `n8nio/n8n:latest`, fixed `n8n_data` root-owned permission issue with `chown 1000:1000`, **added `N8N_SECURE_COOKIE=false` env var** to unlock LAN HTTP access (default in current image is `true` and blocks non-localhost setup page; documented as a quirk in `architecture/components/n8n.md`). docker-compose 1.29.2 `--force-recreate` hit `KeyError: 'ContainerConfig'` Python crash against Docker 29.x — workaround captured. IRIS: cloned `dfir-iris/iris-web`, checkout `v2.4.22` (commit `f75e56fb`), copied `.env.model → .env` (the shipped 36-byte `.env` is a stub — postgres won't start without the real env), commented `depends_on:` blocks in `docker-compose.base.yml` via awk pass (matches the documented gotcha from 2026-04-29). Admin password regenerated on first `docker-compose up` and captured from `docker-compose logs app`. Secrets file updated. Workflow simplification per ADR 0007: Slack + entire approval-gate machinery removed; `SOC-Triage-v3.json` is the new canonical (7 nodes, was 18 in v2; webhook GUID `db7245f7-...` preserved so Splunk needed no change). 4 credentials re-bound in fresh n8n (Anthropic/VT/AbuseIPDB/DFIR-IRIS; IRIS API key regenerated). IRIS catalog reverified post-install: IOC type IDs (79/20/90/111/113) and severity IDs (1=Medium / 2=Unspecified / 3=Informational / 4=Low / 5=High / 6=Critical) unchanged from 2026-04-28 baseline — workflow code paths require no schema changes. New gotcha surfaced post-revert: `alert_status_id=1` maps to "Unspecified" on v2.4.22, not "New" as historically documented; workflow's hardcoded value should be re-derived (deferred). A3 sub-project stub written at `subprojects/2026-05-12-enrichment-expansion/` (urlscan.io + URLhaus + IP2Location top candidates; awaiting brainstorm). Successor: Microsoft Sentinel / Azure pivot work.
2026-05-12 — VMware snapshots taken for the two rebuilt VMs: `n8n-installed-working` (12:34 PM) and `IRIS-installed-working` (12:37 PM). User chose the `-installed-working` suffix over my proposed `-installed-2026-05-12` convention — captures known-good-state rather than calendar date. Recorded as the snapshot-naming convention for future use. Also captured: user's stated intent (saved to memory) to migrate lab VMs from VMware Workstation to Azure at some point post-D1, driven by local-PC compute pressure + alignment with the Microsoft/Azure career pivot. Affects how A3 (next sub-project) should be scoped — portable-across-environments preferred.

2026-05-12 — D1 frozen with full chain re-validated on rebuilt lab. ART Test 15 itself failed on Win10-v2 because **AtomicTestHarnesses module install missed during 2026-05-08 rebuild** (TLS 1.2 not explicit-enabled before `Install-Module` → silent PSGallery failure; runbook now documents this in the ART recovery ladder). Workaround: synthetic `powershell.exe -EncodedCommand` invocation via SSH produced the same Sysmon event shape, same SPL match. **Root-cause debug of a 4-hour saved-search dispatch bug** surfaced + documented: when creating a saved search via REST API, the `search` parameter must NOT have a leading `search` keyword — Splunk's runtime prepends its own implicit `search`, producing `search search index=...` which the parser treats as `<search-command> | <literal-search-term "search"> | <rest>`, reducing scan_count from ~14110 to ~10 and dropping result_count to 0. Splunk Web UI's parser tolerates the doubled keyword (returned 2 events interactively); the scheduler does not. Fix: recreate saved search without leading `search` keyword (UI's Save As Alert flow strips it automatically). Validated end-to-end on the rebuilt v3 chain: **IRIS alert #4** produced from a real `powershell.exe -EncodedCommand` event traversing Win10-v2 → Sysmon → UF → Splunk → saved search → webhook → n8n → Claude (decoded base64 to `Write-Host freeze-validation-<guid>` independently, same behavior as alerts #51/#52 on 2026-04-30) → IRIS. Architectural promise from spec §2.7 *(Sysmon-shaped alert traverses the workflow on real production traffic with no n8n changes)* revalidated across the v2→v3 transition. Phase 11 notes (`subprojects/2026-04-30-detection-foundations/notes.md`) capture the four new gotchas + their dispositions. ADR 0007 captures the Slack-removal decision. `current-state.md` updated to reflect rebuilt lab + v3. Project-root `README.md` written as the portfolio front door. **D1 deliverable is frozen as of this entry**; runway clear for the Microsoft / Azure pivot.

2026-05-18 — pre-publish secrets remediation: audit found AbuseIPDB key inlined in `JSON/SOC-Triage-v1.json` between commits `2ebd850`→`c2e05f6` (2026-04-27) and lab-VM password literal in `log.md` (this file, line 27) + two `subprojects/2026-04-27-structured-outputs/{notes,plan}.md` mentions. Working-tree literals replaced with `[REDACTED]` / pointers to `SOC-Automation-Project.md`. `git filter-repo` rewriting history to scrub the same literals from all prior commits before first public push. Vendored `splunk-mcp-main/` removed from tracking and gitignored (upstream linked from README instead). AbuseIPDB key rotated against `api.abuseipdb.com` (live-SaaS exposure); VM passwords + IRIS API token + Claude API + VirusTotal keys deferred to Azure migration window (VM-scoped creds: no internet-facing surface with VMs powered off). See [[runbooks/secrets-management]].

2026-05-19 — Demo-recording prep: end-to-end test run of the SOAR pipeline before recording the portfolio demo video. Three real issues surfaced + fixed, plus two new vault artifacts. **(1) Credential rotation gap.** The 2026-05-18 rotation runbook step "update n8n credentials" had under-specified — when the cmd.exe synthetic fired through the pipeline today, the Anthropic node failed ("Authorization failed - User is inactive"). Resolved by updating Claude/Anthropic + AbuseIPDB + VirusTotal credentials in n8n's Credentials section. VirusTotal specifically went through three accounts: the original hotmail account had received an active deactivation email (re-registration blocked by VT cooldown); the second-attempt gmail account (`owner@example.com`) never received a verification email but the API key itself was valid (confirmed via direct curl returning `Detection ratio: 65/67` against the EICAR hash, isolating the issue as n8n credential wiring rather than VT account state); cleaned up to a single fresh credential pointing at the verified gmail key. **(2) New demo-path detection.** T1059.001's PowerShell-encoded-command synthetic doesn't embed IOCs (the decoded payload is just `Write-Host <UUID>`), so it doesn't exercise the enrichment tools — not ideal for a portfolio demo. Built a sibling detection: T1059.003 - Suspicious cmd.exe IOC References, which matches cmd.exe `/c` invocations whose CommandLine contains an IPv4 literal, SHA-256 hash, or `http(s)://` URL. Synthetic fires `cmd.exe /c "echo IOC_IP=185.220.101.42 && echo IOC_HASH=<EICAR-sha256> && exit"` — Sysmon-shape identical to real dropper tradecraft, no actual network/file write. Both AbuseIPDB (Tor exit, 100% confidence) and VirusTotal (EICAR identification, ~65/72 engines) fire and Claude integrates both into the alert prose. See [[detections/t1059-003-cmd-suspicious-ioc-references]]. **(3) iocs_enriched propagation bug.** Claude consistently omits the schema-required `iocs_enriched` field from `submit_triage_result` calls — even after the system prompt was hardened with imperative instruction + worked example. Root cause: n8n's tool-schema validator does not enforce JSON Schema `required` strictly; Claude judges in-prose citation as sufficient and skips the structured field. Fix: Code-node fallback that derives `alert_iocs[]` from the flat `r.iocs` object (which Claude reliably populates) and attaches `iocs_enriched` metadata when present. IRIS alert's `Enriched IOCs` section now populates regardless of LLM behavior. See [[decisions/0008-iocs-enriched-code-node-fallback]]. **Two D1 runbook gotchas corrected** in §Recoveries: the saved-search throttle suppress-key recipe `_time,host,Image,CommandLine` was broken (CommandLine doesn't exist post-stats — the t1059-001 SPL ends with `stats by _time, host, User, Image` which renames it to `command_lines`); corrected to `_time,host,Image`. Plus a Splunk UI gotcha: the "Suppress results containing field value" textbox only appears when **Trigger="For each result"** (not "Once" — Once mode bundles into Digest alerts and breaks per-row dedup, visible as `Mode: Digest` in Triggered Alerts). Existing T1059.001 saved search was disabled during recording prep to avoid duplicate-alert noise; re-enable as post-recording cleanup. Demo-recording deliverable: full script + recording setup + post-record artifacts at [[sources/session-notes/2026-05-19-demo-video-script]]; test-run state handoff at [[sources/session-notes/2026-05-19-test-run-handoff]]. IRIS alert #64 (synthetic cmd.exe fire post-fix) is the live-fire validation that the chain is recording-ready.

2026-05-23 — P2 (Azure Port) implementation kickoff; see [[subprojects/2026-05-23-azure-port/README]]

2026-05-23 — P2 Splunk VM section complete (Tasks 1-12). vm-soc-v2-splunk: Ubuntu 24.04 / D4s_v3 / Splunk Enterprise 10.4.0 with Developer License (10 GB/day, exp 2026-11-19), Sysmon TA 5.0.0 + Windows TA 10.0.1, mydfir-project index, T1059.001 + Test-Brute-Force saved searches (T1059 cron */5, brute-force disabled placeholder-history). vm-soc-v2-win: Splunk UF 10.4.0 installed (plan gap — Phase 1 had AMA only, not UF), Sysmon UF stanza pointing at 10.0.0.5:9997, service runs as LocalSystem (Sysmon-channel ACL fix). End-to-end verified: 3,337 events indexed from vm-soc-v2-win in first 7 min; synthetic T1059.001 fired at 22:32:43 UTC and was picked up by the saved search at the 22:35 cron tick (resultCount 4→5). Webhook URL is `.invalid` placeholder pending Task 16. AMA service absence on vm-soc-v2-win flagged in [[subprojects/2026-05-23-azure-port/notes#anomaly-worth-follow-up-not-blocking]] — Sentinel-side Phase 1 state may need re-verification but is non-blocking for P2. See [[subprojects/2026-05-23-azure-port/notes]] for the full set of new gotchas (Splunk 10.4 root-deprecation, dpkg postinst incomplete chown, REST API `actions=webhook` master switch, curl `-d` vs `--data-urlencode`, UF MSI required `SPLUNKPASSWORD`, default UF service account lacks Sysmon channel ACL, sc.exe fragility under PowerShell argument parsing, Run Command session-0 GUI app hangs, here-string fragility in Run Command textbox).

2026-05-25 — P2 Task 20 complete: local Splunk VM (`MyDFIR-Splunk`, 192.168.129.131) decommissioned. Robocopy mirror C:\VMs\MyDFIR-Splunk → F:\VMs\MyDFIR-Splunk (43.96 GB / 144 files / byte-count parity verified) → VMware Delete-from-Disk → 15 orphan `(1)`-suffix duplicates manually cleaned in File Explorer → C: free recovered 44.0 GB (49.6 → 93.6 GB). VMDK archive preserves 3 snapshot chains + 2 RAM dumps (`Snapshot2.vmem`, `Snapshot3.vmem` ~9.2 GB each) for fidelity. Splunk SIEM role now lives exclusively on `vm-soc-v2-splunk` in Azure (end-to-end pipeline verified at Task 19, 2026-05-25 22:40 UTC — first IRIS alert `alert_id=1`). VMware "Delete from Disk" gotcha: only deletes files registered in `.vmsd` snapshot index — orphans from prior copy/rename operations require manual cleanup. See [[subprojects/2026-05-23-azure-port/plan]] Task 20.

2026-05-26 — P2 Task 21 complete: local n8n VM (`MyDfir-n8n-vm`, 192.168.129.132) decommissioned. Robocopy mirror C:\VMs\MyDfir-n8n-vm → F:\VMs\MyDfir-n8n-vm (10.82 GB / 20 files / 2 min 9 sec / byte-count parity verified) → VMware Delete-from-Disk → clean (no orphan files) → C: free recovered 10.9 GB (93.6 → 104.5 GB). One pre-flight obstacle: stale `.lck` folder (PID 26074 dead but lock left behind from an earlier unclean VMware shutdown) blocked the copy until removed. n8n SOAR role now lives exclusively on `vm-soc-v2-n8n` in Azure (SOC Triage v3 workflow active, 4 credentials wired, Splunk webhook hits private IP 10.0.0.6:5678 over VNet).

2026-05-26 — P2 Task 22 complete: local IRIS VM (`MyDfir-DIFR-IRIS-VM-v2`, 192.168.129.133) decommissioned. Robocopy mirror C:\VMs\MyDfir-DIFR-IRIS-VM-v2 → F:\VMs\MyDfir-DIFR-IRIS-VM-v2 (15.24 GB / 20 files / 3 min 10 sec / byte-count parity verified) → VMware Delete-from-Disk → clean (no orphans) → C: free recovered 15.2 GB (104.5 → 119.7 GB). Stale `.lck` again (PID 23419, dead, timestamp identical to n8n's stale lock — both from a single unclean VMware/host event on 2026-05-25 23:47:35). On-disk `.vmx` filename is `Ubuntu 64-bit.vmx` (template-default, never renamed to match the display name `MyDfir-DIFR-IRIS-VM-v2`) — VMware quirk, harmless. IRIS case-management role now lives exclusively on `vm-soc-v2-iris` in Azure (v2.4.22 docker-compose, 5 containers healthy, API at https://10.0.0.7/api/*, end-to-end verified via `alert_id=1` at 2026-05-25 22:40 UTC). Note: v1 IRIS state was NOT migrated — Azure IRIS is a fresh install per the 2026-05-25 plan re-order decision (v1's max alert was #64 from the 2026-05-19 demo session; migration cost > benefit).

2026-05-26 — P2 Task 23 complete + decommission phase done: local Win10 endpoint (`MyDfir-Windows10-v2`, 192.168.129.130) decommissioned. Robocopy mirror C:\VMs\MyDfir-Windows10-v2 → F:\VMs\MyDfir-Windows10-v2 (42.06 GB / 20 files / 8 min 19 sec / byte-count parity verified) → VMware Delete-from-Disk → clean (no orphans) → C: free recovered 42.0 GB (119.7 → 161.7 GB). Stale `.lck` once more (PID 35413, dead, same 23:47:35 timestamp — confirms n8n/IRIS/Win10 locks all came from a single unclean VMware/host shutdown event on 2026-05-25). Snapshot inventory was shallow: one differencing layer (`-000001.vmdk` = 23 GB) + one snapshot (`Snapshot1.vmem`/`.vmsn`); plan's "Windows differencing disks" warning was accurate but archiving as-is preserves restore capability. Endpoint role now lives exclusively on `vm-soc-v2-win` in Azure (Sysmon 15.20 + Splunk UF 10.4.0 forwarding to `vm-soc-v2-splunk` 10.0.0.5:9997). **Decommission phase (Tasks 20-23) summary: C: free 49.6 → 161.7 GB (+112.1 GB recovered, matches sum of 4 VM sizes 44+11+15+42). All four VMDK archives at F:\VMs\ preserving snapshot chains + RAM dumps. Three out-of-scope VMs remain in C:\VMs (MyDfir-FlareVM, MyDfir-Remnux, MyDfir-Zeek-Suricata) untouched.** Next: Tasks 24-26 (latency comparison, runbook, component-doc updates).

2026-05-26 — Back-ported `T1059.003 - Suspicious cmd.exe IOC References` saved search to Azure Splunk (had only existed on the decommissioned local Splunk per 2026-05-19 demo session). Direct stanza append to `/opt/splunk/etc/users/mydfir/search/local/savedsearches.conf` + `sudo systemctl restart Splunkd`; webhook URL points at same n8n private-IP path as T1059.001.
2026-05-26 — P2 Task 24: IOC-rich fire executed (T1059.003 cmd.exe synthetic with IP `185.220.101.42` + EICAR SHA-256 + `.invalid` URL). Fired 16:23:42 UTC → IRIS alert `alert_id=217` at 16:25:23 UTC; ~22 s active processing on D2s_v3 with both AbuseIPDB (confidence=100, Tor exit) + VirusTotal (last_analysis_stats) enrichment exercised. Latency comparison written at [[subprojects/2026-05-23-azure-port/comparison-latency]]; v1 baseline documented honestly as "not measured." New finding: empty structured `iocs[]` in IRIS alert despite full prose IOC citation = Claude system-prompt judgment call (synthetic-event detection), not a wiring bug.
2026-05-26 — P2 Task 25: Operational runbook published at [[subprojects/2026-05-23-azure-port/runbook]]. 21-entry gotcha catalog across Splunk / Splunk UF / n8n / IRIS / Azure Run Command / VMware decommission classes; common ops covered (SSH, web UIs, restarts, manual saved-search dispatch with `trigger_actions=1`, IOC fire, recent-alerts query).
2026-05-26 — P2 Task 26: Component docs (splunk.md, n8n.md, dfir-iris.md, sysmon.md, splunk-mcp.md) and `architecture/current-state.md` updated for Azure topology. Mermaid data-flow diagram swapped from local 192.168.129.x to Azure VNet 10.0.0.x with the 4 P2 VMs + 3 external API tools (Anthropic/VT/AbuseIPDB). v1 host references preserved as "decommissioned, archived to F:\VMs\" callouts.
2026-05-26 — **P2 (Azure Port) COMPLETE.** All 4 local VMs decommissioned; Azure stack operational; T1059.003 IOC enrichment validated end-to-end; comparison-latency + runbook + component docs all published. P2 sub-project closed. Next planned: Phase 3 — Microsoft-native SOAR rewrite on `v3-microsoft-native` branch. See [[subprojects/2026-05-23-azure-port/README]].
2026-07-11 — Honeypot Phase 3 de-obfuscation triage: live paid run. Corpus 5/6 verdict==label on the first run; the 6th (a mixed plaintext+ROT13 sample) surfaced a corpus bug and was relabeled to `unknown` + regression-locked, the byte-exact gate correctly refused the real model's semantic decode. Pulled 3 real captured `-EncodedCommand` from Splunk (self-authored T1059.001 validation, two SYSTEM-run), all builtin-decoded and `clean`, zero false positives. See [[detections/t1059-001-powershell-encoded]] (Phase 3 section).
